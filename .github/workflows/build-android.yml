name: Build Android APK (Capacitor)

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Source Code
      uses: actions/checkout@v4

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: 18

    - name: Prepare Web Assets Folder
      run: |
        mkdir -p www
        find . -maxdepth 1 -name "index.html" -exec cp {} www/ \;
        find . -maxdepth 1 -name "style.css" -exec cp {} www/ \;
        find . -maxdepth 1 -name "game.js" -exec cp {} www/ \;
        find . -maxdepth 1 -name "storage.js" -exec cp {} www/ \;
        
        if [ -d "assets" ]; then
          cp -r assets www/
        fi

    - name: Install Capacitor & Dependencies
      run: |
        npm init -y
        npm install @capacitor/core @capacitor/cli @capacitor/android

    - name: Initialize Capacitor
      run: npx cap init "CarGame" "com.yourname.cargame" --web-dir=www

    - name: Add Android Platform
      run: npx cap add android

    # 🔥 پچ جدید و ۱۰۰٪ ایمن: تنظیم استراتژی رزولوشن در فایل تنظیمات ریشه بدون خراب کردن سینتکس
    - name: Secure Dependency Resolution Strategy
      run: |
        echo "org.gradle.dependency.verification=off" >> android/gradle.properties
        echo "org.gradle.daemon=false" >> android/gradle.properties

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        distribution: 'zulu'
        java-version: '17'

    - name: Sync Web assets to Android
      run: npx cap sync

    # 🎯 شلیک نهایی: با پاس دادن مستقیم پارامتر به گریدل، مجبورش می‌کنیم نسخه ۱.۷۴ را به جای نسخه خراب استفاده کند
    - name: Build Android APK
      run: |
        cd android
        ./gradlew clean
        ./gradlew assembleDebug -Pandroid.security.crypto.version=1.74 --no-build-cache --no-daemon

    - name: Upload APK Artifact
      uses: actions/upload-artifact@v4
      with:
        name: CarGame-Debug-APK
        path: android/app/build/outputs/apk/debug/app-debug.apk
